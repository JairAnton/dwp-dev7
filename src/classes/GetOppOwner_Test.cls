@isTest
public class GetOppOwner_Test {
    
    @isTest
    public static void positiveTest() {
        Account acc1 = TestFactory.createAccount();
        //TestFactory.createUser('Test', 'Ejecutivo');
        //TestFactory.createOpportunity(accountId, userId)
    }

}