public class Contact_Handler {
   
    
    public static void setDefaultCurrency(list<Contact> Contacts){
      Contact_Group_Handler.setDefaultCurrency(Contacts);
    }
    
    public static void createGroupContact(list<Contact> Contacts){
      Contact_Group_Handler.createGroupContact(Contacts); 
    }
}